# AstrBot 插件模板

一个为 AstrBot 设计的自定义插件模板，基于官方基础模板扩展而来。

## 功能描述

`helloworld` 指令：当用户发送 `/helloworld` 时，返回用户的昵称和 QQ 号，以及用户发送的消息内容。

## 指令说明

### helloworld

```
/helloworld
```

返回格式：`Hello, {user_name} (QQ: {user_id}), 你发了 {message_str}!`

## 配置项

本插件无需配置。